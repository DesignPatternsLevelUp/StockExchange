version: '3.9'

services:
  db:
    image: postgres:16
    ports:
      - 5432:5432
    restart: always
    shm_size: 128mb
    environment:
      - POSTGRES_PASSWORD=dev_password
      - POSTGRES_USER=dev_admin
      - POSTGRES_DB=StockExchange
